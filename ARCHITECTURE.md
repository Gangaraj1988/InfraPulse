# Architecture

User → EC2 Instance → Docker Container → Flask Application

Components:

- EC2: Cloud infrastructure
- Docker: Container runtime
- Flask: Application layer
- Bash scripts: Monitoring layer
